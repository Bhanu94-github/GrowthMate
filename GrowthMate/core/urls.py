# core/urls.py
from django.urls import path

urlpatterns = [
    # All API routes are handled by core.api_urls
    # All frontend routes are handled by the project-level catch-all in GrowthMate/urls.py
]